#include <iostream>
using namespace std;

int main() {
    int age;
    do {
        cout << "Enter your age: ";
        cin >> age;
    } while(age < 18 || age > 45);
    cout << "Age is within the valid range." << endl;
    return 0;
}
#include <iostream>
using namespace std;

int main() {
    int age;
    do {
        cout << "Enter your age: ";
        cin >> age;
    } while(age < 18 || age > 45);
    cout << "Age is within the valid range." << endl;
    return 0;
}
